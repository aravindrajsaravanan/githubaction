name: 06-scheduled run
on:
  workflow_dispatch:
  schedule:
    - cron: '7 * * * *'  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: housekeeping
        run: echo "run schedule job at $(date -u)"
